import React, { useState, useEffect } from "react";
import { Heading } from "../Heading/Heading";

export const TotalHire = (content) => {
    return(
        <div>
            <Heading content={hiredPercentage}>{hiredPercentage}</Heading>
        </div>
    )
}